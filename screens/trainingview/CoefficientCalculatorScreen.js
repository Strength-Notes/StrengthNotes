import React from 'react';
import { StyleSheet, View } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

const CoefficientCalculatorScreen = () => (
  <View style={styles.container} />
);

export default CoefficientCalculatorScreen;
